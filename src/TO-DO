-- AXIOS 
-- BUGS : update genre and update authors : when updating, original genres and authors are removed and replaced by the new ones

-- REFACTOR : create components (buttons, forms, etc.) to avoid code duplication
-- REFACTOR : create a custom hook for form handling to avoid code duplication

--  
-- REFACTOR : WHEN A BOOK IS ALREADY IN THE USERLIST, REPLACE THE ADD BUTTON BY TEXT "ALREADY IN YOUR BOOKS"
-- REFACTOR : WHEN ADDING A BOOK TO MY LIST, IF ALREADY IN THE LIST : error message not a system error but a user error
-- REFACTOR : WHEN ADDING A BOOK IN LIST, REDIRECT USER TO HOMEPAGE

-- REFACTOR : when adding a book in the general BOOKSTACK, redirect user to BOOK DETAIL PAGE
-- REFACTOR : ADMIN . AFTER MODIFYING A BOOK, REDIRECTED TO HOME - SHOULD BE REDIRECTED TO BOOK MANAGAMENT PAGE

-- NEW FUNCTIONS to create :
-- VISIT OTHER USER'LISTS 
-- BOOK CARDS : ADD AVERAGE RATING 
-- BOOK DETAIL PAGE : add all comments ans ratings from all users

-- ++++ AUTHOR SERVICE : getAuthorById / update / delete author 
-- +++++ GENRE SERVICE ++++ :  update / delete

